using System;
using System.Collections.Generic;

class RemoveNames
{
    static void Main()
    {
        List<string> Names1 = new List<string>();
        string[]words1 = Console.ReadLine().Split();
        for (int i = 0; i < words1.Length; i++)
        {
            Names1.Add(words1[i]);
        }
        List<string> Names2 = new List<string>();
        string[] words2 = Console.ReadLine().Split();
        for (int i = 0; i < words2.Length; i++)
        {
            Names2.Add(words2[i]);
        }
        List<string> NamesCommon = new List<string>();
        foreach (var item in Names1)
        {
            if (Names2.Contains(item))
            {
                continue;
            }
            else
            {
                NamesCommon.Add(item);
            }
        }
        foreach (var item in NamesCommon)
        {
            Console.Write(item+" ");
        }
        Console.WriteLine();
    }
}

